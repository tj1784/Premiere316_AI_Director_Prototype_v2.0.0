# Pi video-generation repair assignment

User request: deploy a local coding agent to fix actual video generation for Premiere316, preserving the real local workflow through generated motion and eventual export. Current test story: a 180-second Exodus 14 adaptation covering Moses, parted sea, dry crossing, Egyptian pursuit, and the returning sea drowning the pursuing army.

Work in the isolated `codex/pi-video-repair` checkout at `D:/Projects/Premiere316_pi_video_repair`. The primary agent is changing Intake, model planning, QA controls and image preparation in the main checkout. Do not edit that checkout or its files. Do not commit, push, merge, change global Pi configuration, or interrupt the main app/model processes. Write a reviewable patch and a concise evidence report.

Investigate the actual video blockers first. `desktop/workers/ltx25_jsonl_worker.py` and `minimax_h3_jsonl_worker.py` are stubs returning ADAPTER_UNAVAILABLE. `src/lib/studio/video-runtime.ts` blocks them. Identify installed usable native video model components and runtime dependencies. Read the existing adapters, backend authorization and provenance contracts before implementing. Work only on video workers, video adapters/runtime, and focused companion tests/docs; request integration through the final report if a shared UI/backend file needs a coordinated change.

Implement a real local native video generation path if the necessary compatible components exist. Preserve native generation authorization, one-use grants, backend authority validation, model identity, hashes, provenance, memory release and reviewed-iteration controls. Do not replace blockers with success flags, fabricate output or timing, masquerade a still/slideshow as generated motion, or use ComfyUI/port 8188/cloud APIs. Do not download large new models or install system-wide runtimes without reporting exact requirements first. Never expose environment secrets or read credential files. Use PowerShell consistently for file operations; do not issue recursive deletion. Do not change unrelated files.

Validate changes with meaningful focused tests. Real generation is subject to GPU scheduling by the primary agent; do not run GPU workloads while its image test is active. Your report must identify files changed, tests/results, remaining blockers and the exact runtime/checkpoint needed. If no compatible local model/runtime exists, document that concrete evidence instead of claiming repaired video.

Requested 6 GB model diagnosis: LM Studio catalog key `deepseek-v4` points to `antirez/deepseek-v4-gguf/DeepSeek-V4-Flash-Vision-Exp-DSpark-support.gguf` (5,989,114,528 bytes). Loading failed with `unknown model architecture: deepseek4-dspark`. The author's [DSpark documentation](https://github.com/antirez/ds4/blob/main/docs/SPECULATIVE_DECODING.md) explicitly identifies this exact file as support for a separate matching main model, not a standalone LLM. The user subsequently authorized an available loaded Qwen instead. The only loaded Qwen is `qwen3.6-40b-claude-4.6-opus-deckard-heretic-uncensored-thinking-neo-code-di-imatrix-max`, now configured for this isolated Pi agent with thinking off.

For this first bounded pass, inspect the video path and make a concrete repair where supported, then stop with your report at `docs/release/pi-video-repair-report.md`. Use at most 12 tool calls. No GPU inference during this pass; the primary agent owns image generation. Focus on useful checked code or precise missing-component requirements, not a generic plan. You are running on Windows and have a powershell tool. Use it instead of bash.
