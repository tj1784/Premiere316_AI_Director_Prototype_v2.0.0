const ROOT = "D:/Projects/Premiere316_v3";
const BASE = "527046e0b4ee45031fde3b740a9fcfac1f21f534";
const A07 = "C:/Users/teeja/.pi/agent/sessions/--D--Projects-Premiere316_v3--/subagent-artifacts/35f4d972-8675-41a4-bd6e-d1e069c8018a_oracle_0_output.md";
const A64 = "C:/Users/teeja/.pi/agent/sessions/--D--Projects-Premiere316_v3--/subagent-artifacts/8b03856e-cc5c-4206-bfa4-876aac6300dc_oracle_0_output.md";
const schema = { type: "object", additionalProperties: false, required: ["auditor", "verdict", "blockerKind", "summary", "findings"], properties: { auditor: { type: "string" }, verdict: { type: "string", enum: ["pass", "blocked"] }, blockerKind: { type: "string", enum: ["none", "code", "components", "external-runtime", "migration", "data", "security", "ui", "accessibility", "evidence", "packaging"] }, summary: { type: "string" }, findings: { type: "array", items: { type: "string" }, maxItems: 70 } } };
function t(x) { return x.join(" "); }
function common(role) { return t(["TARGET " + ROOT + " protected Wave3 base " + BASE + " plus current uncommitted Wave4 diff. Role " + role + ".", "Preserve 64 logical roles/concurrency<=5/one writer, protected exe/tags/history/IDs/The Last Reel/premiere316-v302-c, Electron sandbox/contextIsolation/webSecurity, responsive 390px and 100-200% zoom.", "No cloud/auth/database/download/model conversion; never mutate model/runtime/Python/cache/live data/ambient processes; never contact ComfyUI/8188. Source only: no Python/model/CUDA/Electron package execution or external writes. Existing Wave4 worker/package source is expected. Wave5 closed."]); }
const writer = await runs.run("wave4-last-source-writer", {
  agent: "worker", acceptance: false, output: "implementation/wave4-last-source-repair.md", outputMode: "file-only", maxRuntimeMs: 18000000,
  task: common("A32/A33/A34 sole targeted repair writer") + t([
    "Read AGENTS.md, full diff, A07 " + A07 + " and A64 " + A64 + ". Fix every listed blocker exactly and preserve all passed work.",
    "Isolate the authority-review modal session. Give its BrowserWindow a unique non-persistent partition tied to its random nonce (no persist: prefix), install modal permission/network handlers only on that isolated session, and prove they cannot alter session.defaultSession handlers or block the product loopback UI/file-root guard. Keep sender+nonce checks, strict modal webPreferences/CSP, no navigation/network and package resources. Add regression source tests for partition isolation.",
    "Make rejection symmetric and main-owned: public renderer Reject calls main; main asks backend to proposeCanonicalRejection, displays an explicit native confirmation with backend-derived authority/asset/iteration/receipt/reason summary, and only native Confirm invokes internal confirmCanonicalRejection. Proposal is one-use; cancel appends nothing; backend revalidates current authority/root/receipt and proposal digest before signed rejectionDecision. Remove direct image.rejectCanonical backend dispatch from main/public reach. Test forged/direct/cancel/replay/cross/stale rejection and reject-then-approve.",
    "Wire backend status as sole UI authority. productionAuthorityStatus must scope canonical/rejection history and prepared approvals strictly to requested picture and current authority; include bounded verified prepared root IDs/digests and decisions only after ledger/MAC/current checks. Never return cross-project history.",
    "PreparedAssetsPanel must call desktopProductionAuthorityStatus on mount and authority changes, render backend CURRENT/NO_AUTHORITY/INVALID truth, and enable Approve prepared only when exact authority ID+digest and prepared root status reconcile with backend. GenerateStage and ReviewStage must independently fetch/re-fetch backend status on mount and immediately before every privileged approve/generate/reject/canonical action; localStorage CURRENT/APPROVED_PREPARED/proof hints alone never enable a button. On mismatch show explicit reseal/reconcile status and fail closed. After prepare/canonical/rejection operations refresh status/history. Persisted hints remain INVALID until the backend response proves exact currentness.",
    "Backend canonical/rejection ledger remains the only authority. UI canonical badges/history derive from scoped backend canonicalHistory, not local approvedIterationId/proof. Keep browser local decisions display-only and unable to grant privilege. Add tests forging local CURRENT/root/proof while backend status is missing/mismatched and verify controls/functions fail closed; verify restart reconciliation and cross-picture history isolation.",
    "Run npm test, typecheck, build, node syntax, JSON and diff checks. Do not run lint/Python/package/inference/commit/stage. Keep Wave4 SOURCE_READY_AWAITING_REVIEW and Wave5 closed."
  ])
});
const reviews = await runs.all([
  { key: "wave4-last-a07", agent: "oracle", acceptance: false, outputSchema: schema, maxRuntimeMs: 9000000, task: common("A07 final source auditor") + " Inspect full diff and " + writer.output + ". Run required source gates. Verify unique modal partition cannot affect defaultSession, status reconciliation is actually called by Inventory/Generate/Review, history is picture+authority scoped, native-confirmed rejection works, and all prior passed source/component/worker/UI contracts remain. No Python/model/package/mutation." },
  { key: "wave4-last-a64", agent: "oracle", acceptance: false, outputSchema: schema, maxRuntimeMs: 9000000, task: common("A64 final source red team") + " Re-run forged local CURRENT/root/proof/canonical/rejection attacks and cross-project status leakage; attempt direct/canceled/replayed rejection and modal session pollution. Verify every privileged action fresh-checks backend ledger truth, canonical/rejection authority remains backend-only, and prior seal/generation/path/network/auto-load controls remain. Run source gates; no Python/model/package/mutation." }
]);
return { status: reviews.every(function (x) { return x && x.structuredOutput && x.structuredOutput.verdict === "pass"; }) ? "WAVE4_SOURCE_GREEN_READY_FOR_PACKAGE" : "BLOCKED_WAVE4_SOURCE", completedGreenThrough: 3, reviews: reviews.map(function (x) { return x && x.structuredOutput ? x.structuredOutput : null; }) };
