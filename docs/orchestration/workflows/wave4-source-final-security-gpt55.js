const ROOT = "D:/Projects/Premiere316_v3";
const BASE = "527046e0b4ee45031fde3b740a9fcfac1f21f534";
const A64 = "C:/Users/teeja/.pi/agent/sessions/--D--Projects-Premiere316_v3--/subagent-artifacts/db680bcc-5c80-4a9d-8cd9-347598cddb3b_oracle_0_output.md";
const schema = {
  type: "object", additionalProperties: false,
  required: ["auditor", "verdict", "blockerKind", "summary", "findings"],
  properties: { auditor: { type: "string" }, verdict: { type: "string", enum: ["pass", "blocked"] }, blockerKind: { type: "string", enum: ["none", "code", "components", "external-runtime", "migration", "data", "security", "ui", "accessibility", "evidence", "packaging"] }, summary: { type: "string" }, findings: { type: "array", items: { type: "string" }, maxItems: 80 } }
};
function t(x) { return x.join(" "); }
function common(role) { return t(["TARGET " + ROOT + " protected Wave3 base " + BASE + " plus current uncommitted Wave4 diff. Role " + role + ".", "Preserve exactly 64 logical roles, physical concurrency <=5, one writer, protected exe/tags/history/IDs/The Last Reel/premiere316-v302-c, Electron sandbox/contextIsolation/webSecurity, 390px and 100-200% zoom.", "No cloud/auth/database/download/model conversion; never mutate D:/AI/Models, installed runtime/Python/cache roots, live operator data or ambient processes; never contact ComfyUI/8188. Source-only means no Python/model/CUDA/package execution or external mutation; the already-created Wave4 worker source and package-resource declarations are expected source and must not be reverted. Wave5 closed."]); }
const writer = await runs.run("wave4-final-security-writer", {
  agent: "worker", acceptance: false, output: "implementation/wave4-final-source-security.md", outputMode: "file-only", maxRuntimeMs: 25200000,
  task: common("A32/A33/A34 sole final source writer") + t([
    "Read AGENTS.md, design-ui, complete diff, authority plan/report in the ee2b0d00 workflow, and full A64 report " + A64 + ". Finish the partial work; full npm test currently has four failures and all must become green without weakening security assertions.",
    "Use A64's explicitly acceptable full-review alternative for the initial untrusted production proposal. Backend must continue stripping caller status/readiness/roots/receipts/graph and constructing its normalized bounded authority projection. Return to Electron main a deterministic full review document containing every privileged field/byte representation that will be sealed: picture and screenplay/source boundary, every active asset id/name/category, exact canonical spec JSON and approved spec version, every normalized reference and digest, every visual/cinematography root, prepared prompt/negative requirements, exact dependency fingerprints/digests, and final projection digest. Do not truncate the review document; reject over-bound proposals instead.",
    "Replace the count/digest MessageBox with a main-owned modal BrowserWindow dedicated to authority review. Add a packaged preload used only by this modal. It renders the complete backend review document in a searchable/scrollable readable view, labels it honestly as an untrusted project proposal normalized by the backend, and has Cancel / Seal exactly reviewed authority controls. Keep contextIsolation:true, nodeIntegration:false, sandbox:true, webSecurity:true, no navigation/window/network/permissions, strict CSP. Use a random one-use main-held nonce and verify event.sender is exactly the modal webContents; primary renderer/preload cannot call the internal modal confirmation. Closing/canceling creates no authority. Include this preload in ASAR/package audits. Add source tests proving full fields are present, no truncation/sample-only summary, modal isolation/nonce/sender checks, and no auto-confirm.",
    "After a sealed authority exists, preserve ID-only prepared approval/generate/canonical routes. No renderer production/prompt/spec/dependency/output metadata may enter those privileged calls. Keep stale/superseded authority denial.",
    "Move canonical truth wholly behind the backend ledger. Do not ask a renderer pure function to authenticate an HMAC it cannot know. Remove/disable the renderer-side approveCanonicalIteration primitive as an authority mutation; it must never set GeneratedIteration.status=APPROVED or asset.approvedIterationId from a proof-shaped object. Backend confirmCanonicalApproval appends the only authoritative signed canonicalDecision. Add narrow read-only image.canonicalStatus/history returning backend-verified ledger projection for UI display after each decision and restart. UI renders canonical status from that live backend result; persisted local hints are explicitly non-authoritative and hydration treats them unverified. Future privileged consumers must use backend authority IDs/status, never local approvedIterationId.",
    "Make Reject equally append-only in backend: visible Review reject invokes a narrow ID-only backend rejection request with current authority/root/receipt/iteration and reviewer reason; backend validates ancestry/current receipt, appends signed rejectionDecision, and prevents canonical approval of that receipt. Then local renderer may append a non-authoritative display review. Backend canonical status returns both rejection and canonical history. Add reject-then-approve denial and restart tests.",
    "The backend canonical request still derives full continuity list/output/reason digests and verifies durable bytes. Return signed decision projection for display, but renderer data can never grant privilege. Remove any comments/labels claiming browser MAC verification. applyProductionAuthority/applyPreparedApproval are UI hints only; rename/comment accordingly and ensure every privileged backend call validates ledger records independently. On hydration they remain INVALID until authorityStatus confirms current.",
    "Repair the four current test failures correctly: additive persistence round-trip expectation; stale prepared invalidation expectation; canonical fixtures/digest ordering now that canonical state is backend-owned; and local-still-policy test should assert renderer prompt is absent from post-seal privileged calls and backend derives prompt from sealed authority—not restore params.prompt trust. Add regressions for forged raw proposal plus meaningful full modal review, forged local approved state/proof having zero backend effect, canonical/rejection status from ledger only, exact reason/output/receipt/continuity binding, and all prior security tests.",
    "Run only source gates: npm test, npm run typecheck, npm run build, node --check relevant JS/CJS, orchestration JSON parse, git diff --check. Do not run npm lint unless separately reporting baseline; it is not this gate. Do not run Python/package/inference, commit/stage, or touch external roots. Keep Wave4 SOURCE_READY_AWAITING_REVIEW and Wave5 closed."
  ])
});
const reviews = await runs.all([
  { key: "wave4-final-a07", agent: "oracle", acceptance: false, outputSchema: schema, maxRuntimeMs: 10800000, task: common("A07 independent source auditor") + " Inspect full diff and " + writer.output + ". Run npm test/typecheck/build/node/JSON/diff gates (not unrelated baseline lint). Trace complete full-detail main-owned authority review, ID-only downstream path, backend-only canonical/rejection ledger truth and read-only status UI. Existing Wave4 Python worker source/package declarations are expected source; verify them but do not claim they were executed. No Python/model/package/mutation." },
  { key: "wave4-final-a64", agent: "oracle", acceptance: false, outputSchema: schema, maxRuntimeMs: 10800000, task: common("A64 independent final source red team") + " Re-run forged renderer attack. Verify backend strips claims and main-owned isolated modal presents the complete exact normalized projection so a user meaningfully approves the sole trust anchor; primary renderer cannot confirm. After seal, attack every route for snapshot/prompt/spec/dependency/output injection, stale/cross/replay, fake local authority/root/proof/canonical status, reject-then-approve, reason/output/receipt/continuity tamper, ledger restart/tamper, auto-load/network/8188. Canonical authority must exist only in backend ledger/status, not a forgeable pure browser mutation. Run source gates; no Python/model/package/mutation." }
]);
return { status: reviews.every(function (x) { return x && x.structuredOutput && x.structuredOutput.verdict === "pass"; }) ? "WAVE4_SOURCE_GREEN_READY_FOR_PACKAGE" : "BLOCKED_WAVE4_SOURCE", completedGreenThrough: 3, reviews: reviews.map(function (x) { return x && x.structuredOutput ? x.structuredOutput : null; }) };
